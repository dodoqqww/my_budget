class Transaction {
  final String id;
  final String name;
  final DateTime date;
  final double amount;
  final String desc;
  // photos

  Transaction(this.id, this.name, this.date, this.amount, this.desc);
}
